# see if this is now integrated into automove / automapper w/ global

#map checking
put #goto %1
waitfor YOU HAVE ARRIVED
put study my map


#loop:
#	put study my map
#	waitforre Obvious paths|Obvious exits
#	goto loop